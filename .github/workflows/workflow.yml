name: CI

on:
  push:
    branches:
      - master  # Ensuring it triggers on pushes to the master branch

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Create logs directory
        run: mkdir -p logs  # Create the logs directory if it doesn't exist

      - name: Run config script
        run: |
          python config.py > logs/config.log 2>&1  # Running config.py and logging output to logs/config.log

      - name: Run tests 
        run: |
          pytest --log-cli-level=INFO --log-file=logs/test.log --log-file-level=INFO  # Log test output to logs/test.log
        env:
          PYTHONPATH: ${{ github.workspace }}
